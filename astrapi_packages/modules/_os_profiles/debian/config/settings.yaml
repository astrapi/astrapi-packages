modal_width: 520

fields:
  - type:  section
    label: Build
    cols:  1

  - key:             default_image
    label:            Standard Build-Image
    type:             select
    options_endpoint: /api/builder/images/for-select?module=debian
    default:          "ctl/debian-builder:latest"
    description:      "Docker-Image das zum Bauen der Debian-Pakete verwendet wird"

  - type:  section
    label: APT-Repository
    cols:  1

  - type:  section
    label: GPG-Signierung
    cols:  1

  - key:         signing_key_id
    label:       GPG-Key-ID (Signierung)
    type:        text
    placeholder: "packages@simpsons.lan"
    description: "GPG-Key-ID zum Signieren des APT-Repositorys. Leer lassen = kein InRelease/Release.gpg. Schlüssel muss im GPG-Keyring des Dienst-Benutzers vorhanden sein."

  - type:  section
    label: Paketquellen
    cols:  1

  - key:         pkg_repos
    label:       Paket-Repos
    type:        list
    mono:        true
    placeholder: "https://github.com/astrapi/packages_debian"
    description: "URLs zu Repos mit packages.json (GitHub, GitLab, Gitea/Forgejo – oder direkte .json-URL). Wird alle 5 Min. aktualisiert."

  - type:  section
    label: Mirror-Sync
    cols:  1

  - key:         mirror_trigger_url
    label:       Mirror-Sync-URL
    type:        text
    placeholder: "http://mirror-host:8000/debian/my-repo/sync"
    description: "URL des Mirror-Endpunkts der nach erfolgreichem Paketbau per POST aufgerufen wird. Leer lassen = kein automatischer Sync."
