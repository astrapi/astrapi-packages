[project]
name = "astrapi-packages"
dynamic = ["version"]
description = "Package Control – Web-UI für Arch/Debian/Alpine-Paketbau"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.135.3",
    "uvicorn[standard]==0.42.0",
    "jinja2==3.1.6",
    "APScheduler==3.11.2",
    "pyyaml==6.0.3",
    "python-multipart==0.0.22",
    "cryptography==46.0.6",
    "requests==2.33.1",
    "urllib3==2.6.3",
    "psutil==7.2.2",
    "apispec==6.10.0",
    "astrapi-core>=26.8.12",
]

[project.scripts]
astrapi-packages = "astrapi_packages._cli:main"

[build-system]
requires = ["setuptools>=61", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]

[tool.setuptools.packages.find]
include = ["astrapi_packages*"]

[tool.setuptools.package-data]
astrapi_packages = [
    "app.yaml",
    "navigation.yaml",
    "modules/*/templates/**/*.html",
    "modules/*/schema.yaml",
    "modules/*/modul.yaml",
    "modules/*/settings.yaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]   # pycodestyle, pyflakes, isort, warnings
ignore = ["E501"]                # Zeilenlänge wird vom Formatter gesteuert

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
